
# NUMPY
import numpy as np
# a = [2,3,4,5,6]
# b = np.array((a))
# # print(b)

# các thông số :
# a.ndim : số chiều
# a.shape : kích thước mỗi chiều
# len(a) : độ dài của a
# a.itemsize():
# a.size : số phần tử trong a
# a.dtype:

# khởi tạo mảng
# np.arange(start,end,step)
# a = np.arange(10)
# print(a)
# np.linspace(start,end,numpy)
a = np.linspace(2,10,100)
print(a)
# in 1 s ra cac so
# a = np.ones((2,3))
# b = np.zeros((3,4))
# c = np.eye(10)
# d = np.diag([1,2,3,4,5])# ms trận đường chéo
# # np.random
# np.random.rand : ngẫu nhiên





























